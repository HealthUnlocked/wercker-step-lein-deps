# lein-deps

Executes `lein deps` command do download dependent libraries. Downloaded
dependencies are cached for the future builds to minimize their duration.

The step is dependent on [lein](http://leiningen.org/) tool. 
